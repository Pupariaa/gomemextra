'use strict'

const fs = require('./')
const assert = require('assert')

it('should have stuff', () => {
  assert(fs.sync, '.sync')
  assert(fs.sync.symlink, '.sync.symlink')
})

it('should return things', () => {
  assert(fs.sync.exists('/'), 'exists()')
})
