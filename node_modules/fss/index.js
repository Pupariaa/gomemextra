'use strict'

const fs = require('fs')
const sync = {}
Object.keys(fs).forEach((key) => {
  if (key.endsWith('Sync')) {
    let fn = fs[key]
    let newKey = key.slice(0, -4)
    sync[newKey] = fn
  }
})
fs.sync = sync

module.exports = fs
